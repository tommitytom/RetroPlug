# build target dir
/bin/
/build/
# sanitizer build dirs (tools/run-sanitizers.sh)
/build-tsan/
/build-asan/

# cmake temp files that remain around
CMakeFiles/
cmake_install.cmake
CMakeCache.txt
Makefile
node_modules
# legacy: bundle now lives under build/ui/
ui/bundle.js
# generic temp files
*.a
*.exe
_dpf_empty.c
lv2_ttl_generator
.claude/settings.local.json

# core dumps
core
core.*

# tools/ Python virtualenv (created by tools/lsdj-manual-setup.sh)
/tools/.venv/

# LSDj manual: keep only the source PDF; everything else is regenerated by
# tools/lsdj-manual-setup.sh.
/resources/manuals/lsdj_manual.md
/resources/manuals/lsdj_manual_images/
/resources/manuals/lsdj_index.db

# Reaper project fixtures (.rpp) — derived/large; regenerate each from its
# committed .test.ts + author .lua via the matching `make reaper-*-author` target.
*.rpp

plans.md
# Greenfield package test-bundle output (derived: esbuild -> tjs run / native host)
packages/retroplug-greenfield/.test-build/
packages/retroplug-greenfield/.native-build/
packages/retroplug-greenfield/.ui-build/
