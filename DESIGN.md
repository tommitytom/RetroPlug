# DESIGN.md → moved

The forward-looking architecture design has outgrown one file and now lives in
[`architecture/`](architecture/README.md) — one doc per work-stream, each with
its own status.

- Start at [architecture/README.md](architecture/README.md) — the thesis, the
  layer model, the minimal native contract, and the doc index.
- The as-is system reference moved to
  [architecture/current-state.md](architecture/current-state.md) (was
  `ARCHITECTURE.md`).

This stub is kept only so old links resolve; it has no content of its own.
