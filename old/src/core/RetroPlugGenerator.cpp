#include "RetroPlugGenerator.h"

namespace rp {
	void RetroPlugGenerator::process(orb::AudioBuffer& out, const RetroPlugComponent& comp, RetroPlugStateComponent&) {
		entt::registry& registry = getRegistry();
	}
}
