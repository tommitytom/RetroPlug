{
  "name": "@retroplug/greenfield",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Greenfield, TS-first reimplementation of RetroPlug's orchestration layer (project/systems/paths/recent/config/SRAM/kits), developed test-driven against a mocked native backend. No C++ in the loop — see README.",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "typecheck": "tsc --noEmit"
  }
}
