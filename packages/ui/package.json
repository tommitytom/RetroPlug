{
  "name": "@retroplug/ui",
  "version": "0.1.0",
  "private": true,
  "description": "RetroPlug plugin UI (React). Bundled to QuickJS bytecode and embedded into the plugin binary by the top-level CMake.",
  "dependencies": {
    "@retroplug/retroplug": "workspace:*"
  }
}
