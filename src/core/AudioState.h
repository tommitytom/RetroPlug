#pragma once

/*#include <vector>
#include "core/System.h"
#include "core/SystemProcessor.h"*/

namespace rp {
	/*class AudioState {
	public:
		SystemProcessor processor;

		void addSystem(SystemPtr system) {
			processor.addSystem(system);
		}

		SystemPtr swapSystem(SystemPtr system) {
			SystemPtr oldSystem = processor.removeSystem(system->getId());
			processor.addSystem(system);
			return oldSystem;
		}

		SystemPtr removeSystem(SystemId systemId) {
			return processor.removeSystem(systemId);
		}
	};*/
}
