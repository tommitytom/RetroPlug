#include "RetroPlugGenerator.h"

namespace rp {
	void RetroPlugGenerator::process(fw::AudioBuffer& out, const RetroPlugComponent& comp, RetroPlugStateComponent&) {
		entt::registry& registry = getRegistry();
	}
}
