{
  "name": "retroplug",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build-native:debug": "powershell ../build-web.ps1 debug -Configure",
    "build-native:development": "powershell ../build-web.ps1 development -Configure",
    "build-native:release": "powershell ../build-web.ps1 release -Configure",
    "build-check": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.11",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^5.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
